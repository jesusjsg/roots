services:
  db:
    image: postgres:18-alpine
    container_name: roots_db
    restart: unless-stopped
    volumes:
      - app-db-data:/var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    env_file:
      - .env

volumes:
  app-db-data:
